I"w	<p>The DOM levels are referencing the position of the elements in the heirarchy of the document. As displayed below, <code class="language-plaintext highlighter-rouge">&lt;div&gt;</code> is the parent element and <code class="language-plaintext highlighter-rouge">&lt;h1&gt;</code> is the child element. <code class="language-plaintext highlighter-rouge">&lt;div&gt;</code> would be on level 1 of the DOM and <code class="language-plaintext highlighter-rouge">&lt;h1&gt;</code> would be on level 2 of the DOM.</p>

<figure class="highlight"><pre><code class="language-ruby" data-lang="ruby"><span class="o">&lt;</span><span class="n">div</span><span class="o">&gt;</span>
<span class="o">&lt;</span><span class="n">h1</span><span class="o">&gt;</span><span class="no">Hello</span><span class="o">&lt;</span><span class="sr">/h1&gt;
&lt;/</span><span class="n">div</span><span class="o">&gt;</span>  </code></pre></figure>

<h2 id="nodes">Nodes</h2>

<p>Nodes are broken down into several types.<br /> 
The <code class="language-plaintext highlighter-rouge">Document</code> node is the root document<br />
The <code class="language-plaintext highlighter-rouge">DocumentType</code> node is the document type such as html, js, css etc.<br />
The <code class="language-plaintext highlighter-rouge">Element</code> node is the most common node as it represents a tag such as <code class="language-plaintext highlighter-rouge">&lt;h1&gt;</code> or <code class="language-plaintext highlighter-rouge">&lt;p&gt;</code><br />
The <code class="language-plaintext highlighter-rouge">Attr</code> node is an attribute tag such as <code class="language-plaintext highlighter-rouge">href</code><br />
The <code class="language-plaintext highlighter-rouge">Text</code> node is the content of a element such as basic text.<br />
The <code class="language-plaintext highlighter-rouge">Comment</code> node is when using // or /* to make a comment.   <br /></p>

<h2 id="journal">Journal</h2>

<p>My experience has taught me that the DOM rapidly expands as a site develops. My approach to it, though potentially incorrect, grew rapidly to the point where I had to switch from going horizontal to vertical. For creating my DOM, I used draw.io, a website I found to be very useful for making graphs and diagrams.</p>

<p>You can find the repo for this lab <a href="https://github.com/NMIT-GITHUB/web503-s2-21-lab-four-EzekielBrown">here</a>.</p>

:ET